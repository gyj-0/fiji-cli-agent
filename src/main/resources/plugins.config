# Fiji CLI Harness Agent - Plugin Configuration
# Menu registration for Fiji plugin system
# Source: Doc 6.1.1 - plugins.config format

# Main menu entry
Plugins>CLI Agent, "Start Agent", fiji.agent.FijiAgent

# Submenu entries (future expansion)
# Plugins>CLI Agent, "Batch Processor", fiji.agent.BatchProcessor
# Plugins>CLI Agent, "Skill Manager", fiji.agent.SkillManager
